<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cool.cxc.app.user.mapper.UserMapper">

    <select id="queryUserList" resultType="UserModel" useCache="true">
        select * from user;
    </select>

    <select id="queryUserByPhone" resultType="UserModel">
        select * from user where phone = #{phone}
    </select>

    <select id="queryUserByEmail" resultType="UserModel">
        select * from user where email = #{email}
    </select>

    <select id="queryUserByUserName" resultType="UserModel">
        select * from user where username = #{username}
    </select>

    <select id="queryUserById" resultType="UserModel">
        select * from user where id = #{id}
    </select>

    <insert id="addUser" parameterType="UserModel" useGeneratedKeys="true" keyProperty="id">
        insert into user (username, password, userpic, phone, email, status, create_Time) values(#{username}, #{password}, #{userpic}, #{phone}, #{email}, #{status}, #{createTime});
    </insert>
</mapper>