server:
    post: 8080

swagger2:
    enable: true

spring:
    profiles:
        active: prod
    feign:
        hystrix:
            enabled: true
        client:
            config:
                default:
                connectTimeout: 5000
                readTimeout: 5000
    jackson:
        date-format: yyyy-MM-dd HH:mm:ss
        time-zone: GMT+8

#     mvc:
#         static-path-pattern: /**




